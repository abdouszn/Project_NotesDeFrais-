@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<br/>

<div class="alert alert-danger">
   <strong>@ViewData["erreurMessage"]</strong>
    @if (ViewData["create"] == "true")
    {
        @Html.ActionLink("Creer  " + @ViewData["element"], "Index", @ViewData["element"] , new { id = "dayMonth" })
    }
</div>

